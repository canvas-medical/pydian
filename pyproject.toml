[tool.poetry]
name = "pydian"
version = "0.1.0"
description = "Library for pythonic data mapping"
authors = ["Eric Pan <eric.pan@canvasmedical.com>"]

[tool.poetry.dependencies]
python = "^3.10"
python-benedict = "^0.25.1"

[tool.poetry.dev-dependencies]
black = "^22.3.0"
coverage = "^6.4.1"
isort = "^5.10.1"
mypy = "^0.960"
pytest = "^7.1.2"

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
